{
  "env": {
    "CLAUDE_CODE_FILE_READ_MAX_OUTPUT_TOKENS": "50000",
    "ENABLE_TOOL_SEARCH": "auto:5"
  },
  "_comment_hooks": "NOTE: SessionStart hook requires bash. On Windows, ensure Git Bash or WSL is available, or the hook will fail silently.",
  "_comment_stop_hooks": "NOTE: Stop hooks with type 'prompt' can only approve/block, not inject text into responses. Previous Stop hooks were removed because they incorrectly tried to append reminders. Use CLAUDE.md instructions instead for behavioral guidance.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "bash ./scripts/hooks/session-setup-wrapper.sh",
            "timeout": 10
          },
          {
            "type": "command",
            "command": "node ./scripts/hooks/load-context.js",
            "timeout": 5
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash ./scripts/hooks/pre-commit-check.sh",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
